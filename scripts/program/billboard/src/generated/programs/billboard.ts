/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import {
  getBillboardCodec,
  type Billboard,
  type BillboardArgs,
} from "../accounts";
import {
  getAcquireInstructionAsync,
  getAppendInstructionAsync,
  getClearInstructionAsync,
  getInitialiseInstructionAsync,
  getUpdateCreatorInstructionAsync,
  parseAcquireInstruction,
  parseAppendInstruction,
  parseClearInstruction,
  parseInitialiseInstruction,
  parseUpdateCreatorInstruction,
  type AcquireAsyncInput,
  type AppendAsyncInput,
  type ClearAsyncInput,
  type InitialiseAsyncInput,
  type ParsedAcquireInstruction,
  type ParsedAppendInstruction,
  type ParsedClearInstruction,
  type ParsedInitialiseInstruction,
  type ParsedUpdateCreatorInstruction,
  type UpdateCreatorAsyncInput,
} from "../instructions";

export const BILLBOARD_PROGRAM_ADDRESS =
  "FwNLuU3KfM3C4JESxa3UZWxBrU5y5ExneEs3kd39wk1n" as Address<"FwNLuU3KfM3C4JESxa3UZWxBrU5y5ExneEs3kd39wk1n">;

export enum BillboardAccount {
  Billboard,
}

export function identifyBillboardAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): BillboardAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([202, 110, 74, 104, 87, 241, 119, 9]),
      ),
      0,
    )
  ) {
    return BillboardAccount.Billboard;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "billboard" },
  );
}

export enum BillboardInstruction {
  Acquire,
  Append,
  Clear,
  Initialise,
  UpdateCreator,
}

export function identifyBillboardInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): BillboardInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([100, 174, 191, 60, 2, 131, 195, 28]),
      ),
      0,
    )
  ) {
    return BillboardInstruction.Acquire;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([149, 120, 18, 222, 236, 225, 88, 203]),
      ),
      0,
    )
  ) {
    return BillboardInstruction.Append;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([250, 39, 28, 213, 123, 163, 133, 5]),
      ),
      0,
    )
  ) {
    return BillboardInstruction.Clear;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([162, 198, 118, 235, 215, 247, 25, 118]),
      ),
      0,
    )
  ) {
    return BillboardInstruction.Initialise;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([39, 221, 251, 213, 194, 161, 31, 207]),
      ),
      0,
    )
  ) {
    return BillboardInstruction.UpdateCreator;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "billboard" },
  );
}

export type ParsedBillboardInstruction<
  TProgram extends string = "FwNLuU3KfM3C4JESxa3UZWxBrU5y5ExneEs3kd39wk1n",
> =
  | ({
      instructionType: BillboardInstruction.Acquire;
    } & ParsedAcquireInstruction<TProgram>)
  | ({
      instructionType: BillboardInstruction.Append;
    } & ParsedAppendInstruction<TProgram>)
  | ({
      instructionType: BillboardInstruction.Clear;
    } & ParsedClearInstruction<TProgram>)
  | ({
      instructionType: BillboardInstruction.Initialise;
    } & ParsedInitialiseInstruction<TProgram>)
  | ({
      instructionType: BillboardInstruction.UpdateCreator;
    } & ParsedUpdateCreatorInstruction<TProgram>);

export function parseBillboardInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedBillboardInstruction<TProgram> {
  const instructionType = identifyBillboardInstruction(instruction);
  switch (instructionType) {
    case BillboardInstruction.Acquire: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: BillboardInstruction.Acquire,
        ...parseAcquireInstruction(instruction),
      };
    }
    case BillboardInstruction.Append: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: BillboardInstruction.Append,
        ...parseAppendInstruction(instruction),
      };
    }
    case BillboardInstruction.Clear: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: BillboardInstruction.Clear,
        ...parseClearInstruction(instruction),
      };
    }
    case BillboardInstruction.Initialise: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: BillboardInstruction.Initialise,
        ...parseInitialiseInstruction(instruction),
      };
    }
    case BillboardInstruction.UpdateCreator: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: BillboardInstruction.UpdateCreator,
        ...parseUpdateCreatorInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        {
          instructionType: instructionType as string,
          programName: "billboard",
        },
      );
  }
}

export type BillboardPlugin = {
  accounts: BillboardPluginAccounts;
  instructions: BillboardPluginInstructions;
};

export type BillboardPluginAccounts = {
  billboard: ReturnType<typeof getBillboardCodec> &
    SelfFetchFunctions<BillboardArgs, Billboard>;
};

export type BillboardPluginInstructions = {
  acquire: (
    input: AcquireAsyncInput,
  ) => ReturnType<typeof getAcquireInstructionAsync> & SelfPlanAndSendFunctions;
  append: (
    input: AppendAsyncInput,
  ) => ReturnType<typeof getAppendInstructionAsync> & SelfPlanAndSendFunctions;
  clear: (
    input: ClearAsyncInput,
  ) => ReturnType<typeof getClearInstructionAsync> & SelfPlanAndSendFunctions;
  initialise: (
    input: InitialiseAsyncInput,
  ) => ReturnType<typeof getInitialiseInstructionAsync> &
    SelfPlanAndSendFunctions;
  updateCreator: (
    input: UpdateCreatorAsyncInput,
  ) => ReturnType<typeof getUpdateCreatorInstructionAsync> &
    SelfPlanAndSendFunctions;
};

export type BillboardPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function billboardProgram() {
  return <T extends BillboardPluginRequirements>(client: T) => {
    return {
      ...client,
      billboard: <BillboardPlugin>{
        accounts: {
          billboard: addSelfFetchFunctions(client, getBillboardCodec()),
        },
        instructions: {
          acquire: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getAcquireInstructionAsync(input),
            ),
          append: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getAppendInstructionAsync(input),
            ),
          clear: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getClearInstructionAsync(input),
            ),
          initialise: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitialiseInstructionAsync(input),
            ),
          updateCreator: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getUpdateCreatorInstructionAsync(input),
            ),
        },
      },
    };
  };
}
