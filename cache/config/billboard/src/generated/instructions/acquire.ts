/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BILLBOARD_PROGRAM_ADDRESS } from "../programs";

export const ACQUIRE_DISCRIMINATOR = new Uint8Array([
  100, 174, 191, 60, 2, 131, 195, 28,
]);

export function getAcquireDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ACQUIRE_DISCRIMINATOR);
}

export type AcquireInstruction<
  TProgram extends string = typeof BILLBOARD_PROGRAM_ADDRESS,
  TAccountBillboard extends string | AccountMeta<string> = string,
  TAccountPrevPoster extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBillboard extends string
        ? WritableAccount<TAccountBillboard>
        : TAccountBillboard,
      TAccountPrevPoster extends string
        ? WritableAccount<TAccountPrevPoster>
        : TAccountPrevPoster,
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AcquireInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type AcquireInstructionDataArgs = { amount: number | bigint };

export function getAcquireInstructionDataEncoder(): FixedSizeEncoder<AcquireInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ACQUIRE_DISCRIMINATOR }),
  );
}

export function getAcquireInstructionDataDecoder(): FixedSizeDecoder<AcquireInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getAcquireInstructionDataCodec(): FixedSizeCodec<
  AcquireInstructionDataArgs,
  AcquireInstructionData
> {
  return combineCodec(
    getAcquireInstructionDataEncoder(),
    getAcquireInstructionDataDecoder(),
  );
}

export type AcquireAsyncInput<
  TAccountBillboard extends string = string,
  TAccountPrevPoster extends string = string,
  TAccountCreator extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  billboard?: Address<TAccountBillboard>;
  prevPoster: Address<TAccountPrevPoster>;
  creator: Address<TAccountCreator>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: AcquireInstructionDataArgs["amount"];
};

export async function getAcquireInstructionAsync<
  TAccountBillboard extends string,
  TAccountPrevPoster extends string,
  TAccountCreator extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BILLBOARD_PROGRAM_ADDRESS,
>(
  input: AcquireAsyncInput<
    TAccountBillboard,
    TAccountPrevPoster,
    TAccountCreator,
    TAccountSigner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AcquireInstruction<
    TProgramAddress,
    TAccountBillboard,
    TAccountPrevPoster,
    TAccountCreator,
    TAccountSigner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BILLBOARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    billboard: { value: input.billboard ?? null, isWritable: true },
    prevPoster: { value: input.prevPoster ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.billboard.value) {
    accounts.billboard.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([98, 105, 108, 108, 98, 111, 97, 114, 100]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("billboard", accounts.billboard),
      getAccountMeta("prevPoster", accounts.prevPoster),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAcquireInstructionDataEncoder().encode(
      args as AcquireInstructionDataArgs,
    ),
    programAddress,
  } as AcquireInstruction<
    TProgramAddress,
    TAccountBillboard,
    TAccountPrevPoster,
    TAccountCreator,
    TAccountSigner,
    TAccountSystemProgram
  >);
}

export type AcquireInput<
  TAccountBillboard extends string = string,
  TAccountPrevPoster extends string = string,
  TAccountCreator extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  billboard: Address<TAccountBillboard>;
  prevPoster: Address<TAccountPrevPoster>;
  creator: Address<TAccountCreator>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: AcquireInstructionDataArgs["amount"];
};

export function getAcquireInstruction<
  TAccountBillboard extends string,
  TAccountPrevPoster extends string,
  TAccountCreator extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BILLBOARD_PROGRAM_ADDRESS,
>(
  input: AcquireInput<
    TAccountBillboard,
    TAccountPrevPoster,
    TAccountCreator,
    TAccountSigner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AcquireInstruction<
  TProgramAddress,
  TAccountBillboard,
  TAccountPrevPoster,
  TAccountCreator,
  TAccountSigner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BILLBOARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    billboard: { value: input.billboard ?? null, isWritable: true },
    prevPoster: { value: input.prevPoster ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("billboard", accounts.billboard),
      getAccountMeta("prevPoster", accounts.prevPoster),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAcquireInstructionDataEncoder().encode(
      args as AcquireInstructionDataArgs,
    ),
    programAddress,
  } as AcquireInstruction<
    TProgramAddress,
    TAccountBillboard,
    TAccountPrevPoster,
    TAccountCreator,
    TAccountSigner,
    TAccountSystemProgram
  >);
}

export type ParsedAcquireInstruction<
  TProgram extends string = typeof BILLBOARD_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    billboard: TAccountMetas[0];
    prevPoster: TAccountMetas[1];
    creator: TAccountMetas[2];
    signer: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: AcquireInstructionData;
};

export function parseAcquireInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAcquireInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      billboard: getNextAccount(),
      prevPoster: getNextAccount(),
      creator: getNextAccount(),
      signer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAcquireInstructionDataDecoder().decode(instruction.data),
  };
}
