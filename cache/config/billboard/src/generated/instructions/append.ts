/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BILLBOARD_PROGRAM_ADDRESS } from "../programs";

export const APPEND_DISCRIMINATOR = new Uint8Array([
  149, 120, 18, 222, 236, 225, 88, 203,
]);

export function getAppendDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(APPEND_DISCRIMINATOR);
}

export type AppendInstruction<
  TProgram extends string = typeof BILLBOARD_PROGRAM_ADDRESS,
  TAccountBillboard extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBillboard extends string
        ? WritableAccount<TAccountBillboard>
        : TAccountBillboard,
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AppendInstructionData = {
  discriminator: ReadonlyUint8Array;
  message: string;
};

export type AppendInstructionDataArgs = { message: string };

export function getAppendInstructionDataEncoder(): Encoder<AppendInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["message", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: APPEND_DISCRIMINATOR }),
  );
}

export function getAppendInstructionDataDecoder(): Decoder<AppendInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["message", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getAppendInstructionDataCodec(): Codec<
  AppendInstructionDataArgs,
  AppendInstructionData
> {
  return combineCodec(
    getAppendInstructionDataEncoder(),
    getAppendInstructionDataDecoder(),
  );
}

export type AppendAsyncInput<
  TAccountBillboard extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  billboard?: Address<TAccountBillboard>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  message: AppendInstructionDataArgs["message"];
};

export async function getAppendInstructionAsync<
  TAccountBillboard extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BILLBOARD_PROGRAM_ADDRESS,
>(
  input: AppendAsyncInput<
    TAccountBillboard,
    TAccountSigner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AppendInstruction<
    TProgramAddress,
    TAccountBillboard,
    TAccountSigner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BILLBOARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    billboard: { value: input.billboard ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.billboard.value) {
    accounts.billboard.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([98, 105, 108, 108, 98, 111, 97, 114, 100]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("billboard", accounts.billboard),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAppendInstructionDataEncoder().encode(
      args as AppendInstructionDataArgs,
    ),
    programAddress,
  } as AppendInstruction<
    TProgramAddress,
    TAccountBillboard,
    TAccountSigner,
    TAccountSystemProgram
  >);
}

export type AppendInput<
  TAccountBillboard extends string = string,
  TAccountSigner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  billboard: Address<TAccountBillboard>;
  signer: TransactionSigner<TAccountSigner>;
  systemProgram?: Address<TAccountSystemProgram>;
  message: AppendInstructionDataArgs["message"];
};

export function getAppendInstruction<
  TAccountBillboard extends string,
  TAccountSigner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BILLBOARD_PROGRAM_ADDRESS,
>(
  input: AppendInput<TAccountBillboard, TAccountSigner, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): AppendInstruction<
  TProgramAddress,
  TAccountBillboard,
  TAccountSigner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BILLBOARD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    billboard: { value: input.billboard ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("billboard", accounts.billboard),
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAppendInstructionDataEncoder().encode(
      args as AppendInstructionDataArgs,
    ),
    programAddress,
  } as AppendInstruction<
    TProgramAddress,
    TAccountBillboard,
    TAccountSigner,
    TAccountSystemProgram
  >);
}

export type ParsedAppendInstruction<
  TProgram extends string = typeof BILLBOARD_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    billboard: TAccountMetas[0];
    signer: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: AppendInstructionData;
};

export function parseAppendInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAppendInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      billboard: getNextAccount(),
      signer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAppendInstructionDataDecoder().decode(instruction.data),
  };
}
