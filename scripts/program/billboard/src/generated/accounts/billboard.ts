/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const BILLBOARD_DISCRIMINATOR = new Uint8Array([
  202, 110, 74, 104, 87, 241, 119, 9,
]);

export function getBillboardDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BILLBOARD_DISCRIMINATOR);
}

export type Billboard = {
  discriminator: ReadonlyUint8Array;
  creator: Address;
  poster: Address;
  amount: bigint;
  message: string;
};

export type BillboardArgs = {
  creator: Address;
  poster: Address;
  amount: number | bigint;
  message: string;
};

/** Gets the encoder for {@link BillboardArgs} account data. */
export function getBillboardEncoder(): Encoder<BillboardArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["creator", getAddressEncoder()],
      ["poster", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["message", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: BILLBOARD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Billboard} account data. */
export function getBillboardDecoder(): Decoder<Billboard> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["creator", getAddressDecoder()],
    ["poster", getAddressDecoder()],
    ["amount", getU64Decoder()],
    ["message", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

/** Gets the codec for {@link Billboard} account data. */
export function getBillboardCodec(): Codec<BillboardArgs, Billboard> {
  return combineCodec(getBillboardEncoder(), getBillboardDecoder());
}

export function decodeBillboard<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Billboard, TAddress>;
export function decodeBillboard<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Billboard, TAddress>;
export function decodeBillboard<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Billboard, TAddress> | MaybeAccount<Billboard, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBillboardDecoder(),
  );
}

export async function fetchBillboard<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Billboard, TAddress>> {
  const maybeAccount = await fetchMaybeBillboard(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBillboard<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Billboard, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBillboard(maybeAccount);
}

export async function fetchAllBillboard(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Billboard>[]> {
  const maybeAccounts = await fetchAllMaybeBillboard(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBillboard(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Billboard>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeBillboard(maybeAccount));
}
